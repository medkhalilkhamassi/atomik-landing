'use client';

import SpatialProblemShowcase from '@/components/spatial-product-showcase';

export default function Manifesto() {
    return (
        <section id="manifesto" className="min-h-screen bg-black flex items-center justify-center py-12">
            <SpatialProblemShowcase />
        </section>
    );
}
